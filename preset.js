//hold preset params when imagine initially load

var params={
   ///positioner's
   startPosition:100,
   stopPosition:400,
   travelBackTime:0,

   ///camera's
   numOfStacks:5,
   framesPerStack:89,
   exposureTime:0.0107,
   idleTime:0.7
};

